spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #driver-class-name: net.sf.log4jdbc.DriverSpy
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/xuanke?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Hongkong
    #url: jdbc:log4jdbc:mysql://localhost:3306/xuanke?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Hongkong
  jpa:
   properties:
    hibernate:
      #format_sql: true
      show_sql: true

jwt:
  config:
    key: huanglinjie
    ttl: 3600000
